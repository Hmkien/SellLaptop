@model Blog

@{
    ViewData["Title"] = "Sửa bài viết";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h4 class="fw-bold mb-0"><i class="bi bi-pencil-square me-2"></i>Sửa bài viết</h4>
    <a asp-action="Index" class="btn btn-outline-secondary"><i class="bi bi-arrow-left me-1"></i>Quay lại</a>
</div>

<div class="admin-card">
    <form asp-action="Edit" method="post">
        <input asp-for="Id" type="hidden" />
        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
        <div class="row g-3">
            <div class="col-md-8">
                <label asp-for="Title" class="form-label fw-semibold">Tiêu đề <span class="text-danger">*</span></label>
                <input asp-for="Title" class="form-control">
                <span asp-validation-for="Title" class="text-danger"></span>
            </div>
            <div class="col-md-4">
                <label asp-for="Category" class="form-label fw-semibold">Chuyên mục</label>
                <input asp-for="Category" class="form-control">
            </div>
            <div class="col-md-12">
                <label asp-for="ThumbnailUrl" class="form-label fw-semibold">Ảnh đại diện (URL)</label>
                <input asp-for="ThumbnailUrl" class="form-control">
            </div>
            <div class="col-md-12">
                <label asp-for="Summary" class="form-label fw-semibold">Tóm tắt</label>
                <textarea asp-for="Summary" class="form-control" rows="2"></textarea>
            </div>
            <div class="col-md-12">
                <label asp-for="Content" class="form-label fw-semibold">Nội dung</label>
                <textarea asp-for="Content" class="form-control" rows="10"></textarea>
            </div>
        </div>
        <hr>
        <div class="d-flex gap-2">
            <button type="submit" class="btn btn-brand"><i class="bi bi-check-lg me-1"></i>Cập nhật</button>
            <a asp-action="Index" class="btn btn-outline-secondary">Hủy</a>
        </div>
    </form>
</div>
